import Link from 'next/link';import AppShell from '@/components/AppShell';import {updateRanch} from '@/app/actions';import {getCurrentOrganization} from '@/lib/org';import {notFound} from 'next/navigation';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase}=await getCurrentOrganization();const {data:r}=await supabase.from('ranches').select('id,name,notes,grower_id').eq('id',id).maybeSingle();if(!r)notFound();return <AppShell><div className="topbar"><h1>Edit ranch</h1></div><form action={updateRanch} className="card form-card"><input type="hidden" name="ranch_id" value={id}/><input type="hidden" name="grower_id" value={r.grower_id}/><label><span>Ranch name *</span><input name="name" required defaultValue={r.name}/></label><label><span>Notes</span><textarea name="notes" rows={4} defaultValue={r.notes||''}/></label><div className="form-actions"><button className="btn">Save changes</button><Link className="btn secondary" href={`/ranches/${id}`}>Cancel</Link></div></form></AppShell>}
