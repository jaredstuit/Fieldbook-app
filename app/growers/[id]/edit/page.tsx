import AppShell from '@/components/AppShell';import {updateGrower} from '@/app/actions';import {getCurrentOrganization} from '@/lib/org';import {notFound} from 'next/navigation';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase}=await getCurrentOrganization();const {data:g}=await supabase.from('growers').select('*').eq('id',id).maybeSingle();if(!g)notFound();return <AppShell><div className="topbar"><h1>Edit grower</h1></div><form action={updateGrower} className="card form-card"><input type="hidden" name="grower_id" value={id}/><div className="form-grid"><label><span>Grower name *</span><input name="name" required defaultValue={g.name}/></label><label><span>Contact</span><input name="contact_name" defaultValue={g.contact_name||''}/></label><label><span>Phone</span><input name="phone" defaultValue={g.phone||''}/></label><label><span>Email</span><input name="email" defaultValue={g.email||''}/></label></div><label><span>Notes</span><textarea name="notes" rows={4} defaultValue={g.notes||''}/></label><div className="form-actions"><button className="btn">Save changes</button></div></form></AppShell>}
