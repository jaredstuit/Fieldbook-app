import AppShell from '@/components/AppShell';import FieldHeader from '@/components/FieldHeader';import FieldSamplesSection from '@/components/FieldSamplesSection';import {getFieldContext} from '@/lib/field';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,field,ranch,grower}=await getFieldContext(id);return <AppShell active="Fields"><FieldHeader field={field} ranch={ranch} grower={grower} active="samples"/><FieldSamplesSection supabase={supabase} fieldId={id}/></AppShell>}
