import AppShell from '@/components/AppShell';import FieldHeader from '@/components/FieldHeader';import FieldTimelineSection from '@/components/FieldTimelineSection';import {getFieldContext} from '@/lib/field';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,field,ranch,grower}=await getFieldContext(id);return <AppShell active="Fields"><FieldHeader field={field} ranch={ranch} grower={grower} active="timeline"/><FieldTimelineSection supabase={supabase} fieldId={id}/></AppShell>}
