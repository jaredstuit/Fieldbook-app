import AppShell from "@/components/AppShell";
import FieldHeader from "@/components/FieldHeader";
import { getFieldContext } from "@/lib/field";
export default async function FieldPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,field,ranch,grower,water}=await getFieldContext(id);const [n,o,s,soil]=await Promise.all([supabase.from('field_notes').select('id',{count:'exact',head:true}).eq('field_id',id),supabase.from('observations').select('id',{count:'exact',head:true}).eq('field_id',id),supabase.from('samples').select('id',{count:'exact',head:true}).eq('field_id',id),supabase.from('field_soils').select('id',{count:'exact',head:true}).eq('field_id',id)]);return <AppShell active="Fields"><FieldHeader field={field} ranch={ranch} grower={grower} active="overview"/><div className="grid grid-2"><section className="card"><div className="section-head"><h2>Field overview</h2></div><div className="kv"><div><div className="label">Crop</div><div className="value">{[field.variety,field.current_crop].filter(Boolean).join(' ')||'—'}</div></div><div><div className="label">Rootstock</div><div className="value">{field.rootstock||'—'}</div></div><div><div className="label">Planting year</div><div className="value">{field.planting_year||'—'}</div></div><div><div className="label">Irrigation</div><div className="value">{field.irrigation_type||'—'}</div></div><div><div className="label">Water source</div><div className="value">{water?.name||'—'}</div></div><div><div className="label">Acres</div><div className="value">{field.acres?Number(field.acres).toFixed(2):'—'}</div></div></div>{field.notes?<p className="subtle" style={{marginTop:18}}>{field.notes}</p>:null}</section><section className="card snapshot"><h2>Record snapshot</h2><div className="snapshot-grid"><div><strong>{n.count||0}</strong><span>Field notes</span></div><div><strong>{o.count||0}</strong><span>Observations</span></div><div><strong>{s.count||0}</strong><span>Samples</span></div><div><strong>{soil.count||0}</strong><span>USDA soils</span></div></div><p className="subtle" style={{marginTop:18}}>Use the tabs above to open the full timeline, sample history, notes, or map without loading every record at once.</p></section></div></AppShell>}
