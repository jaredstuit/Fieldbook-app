# Fieldbook v0.6.3 — Update Checklist

1. No new Supabase SQL is required after a successful v0.5 sync.
2. Upload/commit the v0.6.3 project files to the existing private GitHub `Fieldbook` repository.
3. Keep `package.json` at the repository root.
4. Vercel should automatically start a deployment after the GitHub commit.
5. Do not change the existing Vercel environment variables.
6. Verify login, field page, note entry, observation entry, sample entry, timeline, and Samples index.
