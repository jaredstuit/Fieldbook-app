# Fieldbook v0.6.3 clean repository

This package is intended to be uploaded into a brand-new private GitHub repository.

Important:
- Upload the CONTENTS of this folder to the repository root.
- Do not upload this ZIP itself.
- `package.json`, `app/`, `components/`, `lib/`, and `supabase/` must all be visible at the GitHub repository root.
- No Supabase migration is required for this v0.6.x release.
